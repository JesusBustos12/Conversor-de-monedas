PORT=3000
NODE_ENV=production

# Configuración de base de datos
DB_HOST=mysql-db
DB_USER=currency_master
DB_PASS=tu_contrasena_segura
DB_NAME=currencyhub_db

# Seguridad
# Generar uno nuevo con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=tu_secreto_super_seguro_de_64_bytes

# CORS
ALLOWED_ORIGINS=https://localhost,http://localhost:3000
